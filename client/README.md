# Khadra Client (React + Vite + Tailwind)

Run locally:

```bash
cd client
npm install
npm run dev
```

This starts a Vite dev server on `http://localhost:5173` by default. The client proxies calls to the backend at the same origin, so run the backend at `http://127.0.0.1:3000` in parallel.
